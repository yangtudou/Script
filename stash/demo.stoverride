name: "远程脚本注入测试"
desc: "使用 GitHub 脚本动态修改配置"

# 正确的 Overrides 语法：script 下面直接跟脚本名作为键
script:
  node_cleaner:  # 这里不要加横杠 -
    type: config
    script-path: "https://raw.githubusercontent.com/yangtudou/Script/main/stash/demo.js"