# version: v2.1_beta
# updated at: 2025-12-24
# 改用 checkout sparse-checkout
# 改用 rules 分支管理
# 使用外部 shell 脚本处理规则文件, 简化了整个 Github Action 流程
#
# UTC 22:00 (北京时间早 6:00 自动拉取) 
#
# 支持 Surge ClashMeta、Clash Premium
#
# tip: 这里 env 下不能写任何注释

name: 自动化规则下载 v2.1_beta

on:
  # schedule:
  #   - cron: '0 22 * * *'
  workflow_dispatch:
  # push:
  #   branches:
  #     - main
  #   paths:
  #     - ".github/custom_direct.txt"
  #     - ".github/custom_proxy.txt"

permissions:
  contents: write
  actions: write

env:
  METACUBEX_BASE_DIR: "meta-rules-dat"
  METACUBEX_SURGE_PROXY_FILES: |
    geo/geosite/classical/missav.list
    geo/geosite/classical/javbus.list
    geo/geosite/classical/gfw.list
    geo/geosite/classical/pinterest.list
    geo/geosite/classical/github.list
    geo/geosite/classical/meta.list
    geo/geosite/classical/tld-!cn.list
    geo/geosite/classical/cloudflare.list
    geo/geoip/classical/cloudflare.list
    geo/geosite/classical/telegram.list
    geo/geoip/classical/telegram.list
    geo/geosite/classical/google.list
    geo/geoip/classical/google.list
  METACUBEX_SURGE_DIRECT_FILES: |
    geo/geosite/classical/icloud@cn.list
    geo/geosite/classical/apple@cn.list
    geo/geosite/classical/tesla@cn.list
    geo/geosite/classical/tencent.list
    geo/geosite/classical/xunlei.list
    geo/geosite/classical/tld-cn.list
    geo/geosite/classical/cn.list
    geo/geosite/classical/private.list
    geo/geoip/classical/private.list
  METACUBEX_CLASH_DOMAIN_FILES: |
    geo/geosite/cn.yaml
    geo/geosite/meta.yaml
    geo/geosite/apple.yaml
    geo/geosite/google.yaml
    geo/geosite/github.yaml
    geo/geosite/private.yaml
    geo/geosite/telegram.yaml
    geo/geosite/cloudflare.yaml
    geo/geosite/javbus.yaml
    geo/geosite/geolocation-!cn.yaml
  METACUBEX_CLASH_IP_FILES: |
    geo/geoip/cn.yaml
    geo/geoip/google.yaml
    geo/geoip/private.yaml
    geo/geoip/telegram.yaml
    geo/geoip/cloudflare.yaml
  GEOX: |
    cn #aio
    meta
    apple
    google #aio
    github
    private #aio
    telegram #aio
    cloudflare #aio
    geolocation-!cn
jobs:
  rule_sync:
    runs-on: ubuntu-latest
    steps:
      - name: "处理变量"
        run: |
          {
            echo "SURGE_FILES<<EOF"
            while IFS= read -r line; do

            clean_line=$(echo "$line" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
            [[ -z "$clean_line" ]] && continue

              if [[ "$clean_line" == *"#"* ]]; then
                # 有注释的情况
                main_part=$(echo "$clean_line" | cut -d'#' -f1 | xargs)
                comment_part=$(echo "$clean_line" | cut -d'#' -f2- | xargs)
              else
                # 没有注释的情况
                main_part="$clean_line"
                comment_part=""
              fi
            
              # 根据注释决定路径
              if [[ -n "$comment_part" ]]; then
                case "$comment_part" in
                  "aio") 
                    echo "meta-rules-dat/geo/geosite/classical/$main_part.list"
                    echo "meta-rules-dat/geo/geoip/classical/$main_part.list"
                    ;;
                  *)
                    # 未知注释，使用默认路径但记录警告
                    echo "geo/geosite/$main_part.yaml"
                    echo "警告: 未知注释 '$comment_part' 用于文件 '$main_part'" >&2
                    ;;
                esac
              else
                echo "meta-rules-dat/geo/geosite/classical/$main_part.list"
              fi
            done <<< "$GEOX"
            echo "EOF"
          } >> $GITHUB_ENV
          
      - name: "展示变量"
        run: |
          echo "$SURGE_FILES"

          
      # - name: "📥 当前仓库 rules 分支"
      #   uses: actions/checkout@v6
      #   with:
      #     path: rule_sync_temp
      #     ref: rules
      #     fetch-depth: 1

      # - name: "📥 MetaCubeX 仓库 meta 分支"
      #   uses: actions/checkout@v6
      #   with:
      #     repository: MetaCubeX/meta-rules-dat
      #     ref: meta
      #     fetch-depth: 1
      #     path: ${{ env.METACUBEX_BASE_DIR }}
      #     sparse-checkout: |
      #       ${{ env.METACUBEX_SURGE_DIRECT_FILES }}
      #       ${{ env.METACUBEX_SURGE_PROXY_FILES }}
      #       ${{ env.METACUBEX_CLASH_DOMAIN_FILES }}
      #       ${{ env.METACUBEX_CLASH_IP_FILES }}

      #     sparse-checkout-cone-mode: false

      # - name: "📥 AWAvenue 仓库 main 分支"
      #   uses: actions/checkout@v6
      #   with:
      #     repository: TG-Twilight/AWAvenue-Ads-Rule
      #     ref: main
      #     fetch-depth: 1
      #     path: AWAvenue-Ads-Rule
      #     sparse-checkout: |
      #       Filters/AWAvenue-Ads-Rule-Clash.yaml
      #       Filters/AWAvenue-Ads-Rule-Surge-RULE-SET.list

      #     sparse-checkout-cone-mode: false

      # - name: "📥 Loyalsoldier/surge-rules 仓库 release 分支"
      #   uses: actions/checkout@v6
      #   with:
      #     repository: Loyalsoldier/surge-rules
      #     ref: release
      #     fetch-depth: 1
      #     path: Loyalsoldier_surge-rules
      #     sparse-checkout: |
      #       ruleset/apple.txt

      #     sparse-checkout-cone-mode: false

      # - name: "🔧 下载脚本"
      #   run: |
      #     curl -L -o merge_rules.sh https://raw.githubusercontent.com/yangtudou/Script/main/.github/merge_rules_beta.sh
      #     chmod +x merge_rules.sh
 
      # - name: 整理 ClashMeta 规则
      #   run: |
      #     set -e

      #     # 引入外部 shell 脚本
      #     source ./merge_rules.sh

      #     echo "开始整理 ClashMeta 规则..."
      #     rm -rf rule_sync_temp/ClashMeta
      #     mkdir -p rule_sync_temp/ClashMeta/{domain,ip}

      #     # domain
      #     merge_rules "$METACUBEX_BASE_DIR/geo/geosite" "rule_sync_temp/ClashMeta/domain" "directory"

      #     # ip
      #     merge_rules "$METACUBEX_BASE_DIR/geo/geoip" "rule_sync_temp/ClashMeta/ip" "directory"

      #     # AWAvenue-Ads-Rule
      #     merge_rules "AWAvenue-Ads-Rule/Filters/AWAvenue-Ads-Rule-Clash.yaml" "rule_sync_temp/ClashMeta/domain/AWAvenue-Ads-Rule-Clash.yaml" "file"
          
      # - name: 整理 Surge 规则
      #   run: |
      #     set -e

      #     # 引入外部 shell 脚本
      #     source ./merge_rules.sh
          
      #     echo "开始整理 Surge 规则..."
          
      #     if rm -rf "rule_sync_temp/Surge"; then
      #       echo "✓ 目录删除成功: rule_sync_temp/Surge"
      #     else
      #       echo "✗ 删除失败: 权限不足或文件系统错误" >&2
      #       return 1
      #     fi

      #     if mkdir -p "rule_sync_temp/Surge"; then
      #       echo "✓ 目录重建成功: rule_sync_temp/Surge"
      #     else
      #       echo "✗ 目录重建失败: 权限不足或文件系统错误" >&2
      #       return 1
      #     fi

      #     # proxy
      #     curl -L -o "custom_proxy.txt" https://raw.githubusercontent.com/yangtudou/Script/main/.github/custom_proxy.txt
          
      #     action_env_to_array_fix "$METACUBEX_SURGE_PROXY_FILES" "PROXY_RULES" "$METACUBEX_BASE_DIR"
      #     PROXY_RULES+=("custom_proxy.txt")
      #     merge_rules "PROXY_RULES" "rule_sync_temp/Surge/y3_proxy_rules@surge.list" "file"

      #     # direct
      #     curl -L -o "custom_direct.txt" https://raw.githubusercontent.com/yangtudou/Script/main/.github/custom_direct.txt
          
      #     action_env_to_array_fix "$METACUBEX_SURGE_DIRECT_FILES" "DIRECT_RULES" "$METACUBEX_BASE_DIR"
      #     DIRECT_RULES+=("custom_direct.txt")
      #     merge_rules "DIRECT_RULES" "rule_sync_temp/Surge/y3_direct_rules@surge.list" "file"

      #     # AWAvenue-Ads-Rule
      #     merge_rules "AWAvenue-Ads-Rule/Filters/AWAvenue-Ads-Rule-Surge-RULE-SET.list" "rule_sync_temp/Surge" "directory"
      
      # - name: 提交变更
      #   working-directory: rule_sync_temp
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      #     git checkout --orphan temp
      #     git branch -D rules
      #     git checkout --orphan rules
          
      #     git add .
      #     git commit -m "CI: Auto Update at 北京时间 $(TZ='Asia/Shanghai' date +'%Y-%m-%d')"
      #     git push -f origin rules

      # - name: 清除工作流
      #   uses: Mattraks/delete-workflow-runs@v2
      #   with:
      #     retain_days: 0
      #     keep_minimum_runs: 0
      #     repository: ${{ github.repository }}
